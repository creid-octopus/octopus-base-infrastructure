# instance-infrastructure

Provisions the underlying AKS cluster: resource group, node pool, networking. No cluster software installed here — see `terraform/kubernetes-paas/`.

Placeholder — content migrating in from `iac-octopus`.
